{
  "name": "tangsel-tradelink",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "npm run validate:data && astro build",
    "preview": "astro preview",
    "test": "node --test tests/*.test.mjs",
    "validate:data": "node scripts/validate-tenants.mjs"
  },
  "dependencies": {
    "astro": "7.3.0"
  }
}
